//! Sapphire is both a domain specific language and a framework to model financial and investment markets.
//! Sapphire is unique in the sense it creates models from reacting types of cells, similar to what is
//! currently known as "cellular automata".


#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
